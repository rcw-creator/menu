# .env

# Database connection (example for PostgreSQL)
DB_USER=my_user
DB_PASSWORD=my_secure_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=my_database
DB_TABLE=experiment_results

# Output folder for charts and reports
OUTPUT_ROOT=reports_output
